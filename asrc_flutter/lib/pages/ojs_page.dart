import 'package:flutter/material.dart';

class OjsPage extends StatelessWidget {
  const OjsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Open Journal Systems (OJS) Page',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
